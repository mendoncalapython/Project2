apiVersion: v1
kind: Service

metadata:
  name: analytics-service

spec:
  type: NodePort

  selector:
    app: analytics-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30008
